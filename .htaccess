<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # If the request is already for the public directory, do nothing
    RewriteCond %{REQUEST_URI} /public/
    RewriteRule ^ - [L]

    # Otherwise, rewrite everything to the public directory
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
